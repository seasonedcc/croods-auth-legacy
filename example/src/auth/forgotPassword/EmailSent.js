import React from 'react'

import CentralizedMessage from '../../CentralizedMessage'

export default props => (
  <CentralizedMessage>
    <h2>We sent you an email with instructions to reset your password</h2>
  </CentralizedMessage>
)
